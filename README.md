#include<stdio.h>

int main() {
    int aadhar[5];
    for(int i = 0; i < 10; i++) {
        printf("%d index : ", i);
        scanf("%d", (ptr + i));
    }
    // output
    for(int i = 0; i < 10; i++) {
        printf("%d index = %d", i, *(ptr + i));
    }
    return 0;
}
